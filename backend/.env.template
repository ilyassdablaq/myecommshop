# ============================================
# DATABASE & CACHE (Local Development)
# ============================================
# For Railway: These will be auto-generated
# For Upstash Redis: Use your Upstash Dashboard URL
DATABASE_URL=postgresql://user:password@localhost:5432/medusa-v2
REDIS_URL=redis://localhost:6379

# Upstash Redis Example:
# REDIS_URL=https://default:ATWqAAIncDE1OTczOWMyYTc1YTA0OTBlYmY5YjRhYTM4MGEyOGFlNHAxMTM3Mzg@creative-eagle-13738.upstash.io

# ============================================
# SECURITY KEYS (CHANGE IN PRODUCTION!)
# ============================================
# Generate secure keys with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
COOKIE_SECRET=your-super-secret-cookie-key-min-32-chars

# ============================================
# CORS CONFIGURATION
# ============================================
# Local development with multiple origins
STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com

# Production example (update with your domains):
# STORE_CORS=https://shop.example.com,https://docs.medusajs.com
# ADMIN_CORS=https://admin.example.com
# AUTH_CORS=https://admin.example.com

# ============================================
# API & SERVER CONFIGURATION
# ============================================
NODE_ENV=development
MEDUSA_BACKEND_URL=http://localhost:9000
WORKER_MODE=shared

# Database
DB_NAME=medusa-v2
